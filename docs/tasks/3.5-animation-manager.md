# Task 3.5: AnimationManager

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/RENDERER.md` → "Line Clear Animation" (lines 158-193)

## Prerequisites
- [ ] Task 3.1 complete — color constants
- [ ] Task 1.7 complete — `EventBus` for subscribing to LINE_CLEAR events
- [ ] Files exist: `src/renderer/colors.ts`, `src/engine/EventBus.ts`

## Description
Implement line clear animations and lock flash. Subscribes to LINE_CLEAR events on the EventBus. Flash phase (0-150ms): white rectangle expands from center. Dissolve phase (150-400ms): blocks shrink and fade. Stores cleared row data separately so rows above don't collapse visually until animation completes.

## Files
| Action | Path |
|--------|------|
| Create | `src/renderer/AnimationManager.ts` |

## Acceptance Criteria
- [ ] Subscribes to `LINE_CLEAR` event on the EventBus
- [ ] Stores cleared row data (cell types) separately from the board grid
- [ ] Flash phase (0-150ms): white rectangle expands from center, opacity decreases from 0.8
- [ ] Dissolve phase (150-400ms): blocks shrink toward center, fade to transparent, drawn white
- [ ] Rows above cleared lines don't visually collapse until animation completes
- [ ] `update(deltaMs)` advances all active animations
- [ ] `draw(ctx, cellSize)` renders all active animations
- [ ] Completed animations are automatically removed

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 3.7, Task 6.1
