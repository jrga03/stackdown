# Task 1.8: Game Mode Types

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/ENGINE.md` → "Enums" (GameMode enum)
> - `docs/ENGINE.md` → "Constants" (PRACTICE_DURATION_MS)
> - `docs/ENGINE.md` → "Core Types — GameSnapshot" (gameMode, remainingMs fields)
> - `docs/ENGINE.md` → "Event Payloads — EventMap" (GAME_OVER reason, TIME_WARNING)
> - `docs/UI.md` → "useGameSession Hook" (GameConfig interface)

## Prerequisites
- [ ] Task 1.2 complete — base types and constants exist in `types.ts` and `constants.ts`

## Description
Add game mode support to the type system. Define the `GameMode` enum, `PRACTICE_DURATION_MS` constant, `GameConfig` interface, and extend `GameSnapshot` with `gameMode` and `remainingMs` fields. Update `GAME_OVER` event payload to include `reason` and add `TIME_WARNING` event type.

## Files
| Action | Path |
|--------|------|
| Modify | `src/engine/types.ts` |
| Modify | `src/engine/constants.ts` |

## Acceptance Criteria
- [ ] `GameMode` enum has two values: `MARATHON = 'marathon'`, `PRACTICE = 'practice'`
- [ ] `PRACTICE_DURATION_MS` constant equals `120_000`
- [ ] `GameSnapshot` has `gameMode: GameMode` field
- [ ] `GameSnapshot` has `remainingMs: number | null` field (null for marathon, countdown for practice)
- [ ] `GameEventType` includes `TIME_WARNING = 'time_warning'`
- [ ] `EventMap[GAME_OVER]` payload includes `reason: 'topout' | 'timeout'`
- [ ] `EventMap[TIME_WARNING]` payload is `{ remainingMs: number }`
- [ ] `GameConfig` interface defined with `mode: GameMode` and `startLevel: number`
- [ ] All types compile without errors

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 2.8, Task 5.7
