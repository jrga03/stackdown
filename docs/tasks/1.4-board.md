# Task 1.4: Board

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/ENGINE.md` → "Board" (lines 139-175)

## Prerequisites
- [ ] Task 1.2 complete — `Cell`, `Grid`, `Position`, `PieceType`, board dimension constants
- [ ] Files exist: `src/engine/types.ts`, `src/engine/constants.ts`

## Description
Implement the board as a 10x40 grid with collision detection and line clearing. The board is the central data structure that tracks locked pieces. Row 0 is the top (buffer zone), row 39 is the bottom.

## Files
| Action | Path |
|--------|------|
| Create | `src/engine/Board.ts` |
| Create | `src/engine/__tests__/Board.test.ts` |

## Acceptance Criteria
- [ ] Board initializes as a 40-row x 10-column grid of nulls
- [ ] `isValidPosition(blocks)` returns `true` for positions within bounds and on empty cells
- [ ] `isValidPosition(blocks)` returns `false` for out-of-bounds positions (left, right, bottom)
- [ ] `isValidPosition(blocks)` returns `false` for positions overlapping occupied cells
- [ ] `lockPiece(blocks, type)` writes the piece type into the grid
- [ ] `clearFullRows()` removes full rows and shifts rows above down
- [ ] `clearFullRows()` returns the indices of cleared rows (original positions)
- [ ] Clearing 1, 2, 3, and 4 rows simultaneously works correctly
- [ ] `getCell(row, col)` returns the correct cell value
- [ ] All tests pass

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] Tests pass: `npx vitest run src/engine/__tests__/Board.test.ts`
- [ ] No circular imports
- [ ] Unblocks: Task 1.5, Task 2.3, Task 2.6
