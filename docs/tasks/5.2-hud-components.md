# Task 5.2: HUD Components

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/UI.md` → "HUD Components" (lines 155-239)

## Prerequisites
- [ ] Task 5.1 complete — `GameScreen` and `useGameSession` providing game state
- [ ] Task 3.4 complete — `PieceRenderer.drawPiecePreview()` for hold/next rendering
- [ ] Files exist: `src/ui/screens/GameScreen.tsx`, `src/hooks/useGameSession.ts`, `src/renderer/PieceRenderer.ts`

## Description
Create HUD components for score, level, lines, hold piece, and next queue. Score/level/lines are simple text displays. HoldPiece and NextQueue render on separate small canvases and only re-render when their data changes.

## Files
| Action | Path |
|--------|------|
| Create | `src/ui/hud/ScoreDisplay.tsx` |
| Create | `src/ui/hud/LevelDisplay.tsx` |
| Create | `src/ui/hud/LinesDisplay.tsx` |
| Create | `src/ui/hud/HoldPiece.tsx` |
| Create | `src/ui/hud/NextQueue.tsx` |
| Create | `src/ui/hud/HUD.tsx` |

## Acceptance Criteria
- [ ] `ScoreDisplay` shows current score with locale separators
- [ ] `LevelDisplay` shows current level
- [ ] `LinesDisplay` shows total lines cleared
- [ ] `HoldPiece` renders held piece on separate small canvas; empty state shows nothing
- [ ] `HoldPiece` only re-renders when held piece type changes
- [ ] `NextQueue` renders next 5 pieces on separate small canvas
- [ ] `NextQueue` only re-renders when queue contents change
- [ ] `HUD` composes all display components in correct layout

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 5.3
