# Task 3.3: BoardRenderer

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/RENDERER.md` → "Grid Rendering" (lines 113-137)

## Prerequisites
- [ ] Task 3.1 complete — `BOARD_COLORS` for background, grid lines, border
- [ ] Files exist: `src/renderer/colors.ts`

## Description
Implement grid background rendering with OffscreenCanvas caching. The grid cache stores the background fill, vertical lines (11), horizontal lines (21), and outer border. Cache is only regenerated when dimensions change.

## Files
| Action | Path |
|--------|------|
| Create | `src/renderer/BoardRenderer.ts` |

## Acceptance Criteria
- [ ] Grid cache uses `OffscreenCanvas` (with `HTMLCanvasElement` fallback)
- [ ] Grid cache only regenerated when dimensions change
- [ ] Grid includes: background fill, 11 vertical lines, 21 horizontal lines, outer border
- [ ] Grid lines use `BOARD_COLORS.gridLine` color
- [ ] `drawCachedGrid(ctx)` blits the cached grid to the main canvas

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 3.7
