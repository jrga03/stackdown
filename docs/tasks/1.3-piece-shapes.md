# Task 1.3: Piece Shapes

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/ENGINE.md` → "Piece Shapes" (lines 177-306)

## Prerequisites
- [ ] Task 1.2 complete — `PieceType`, `RotationState`, `Position` types defined
- [ ] Files exist: `src/engine/types.ts`

## Description
Define all 7 tetromino shapes in all 4 rotation states as coordinate arrays. Each shape is a `Position[]` relative to the bounding box origin (top-left). Expose a `getBlocks(type, rotation)` function.

## Files
| Action | Path |
|--------|------|
| Create | `src/engine/Piece.ts` |

## Acceptance Criteria
- [ ] Each piece type has exactly 4 rotation states
- [ ] Each rotation state has exactly 4 blocks
- [ ] I piece uses a 4x4 bounding box
- [ ] O piece uses a 2x2 bounding box (same shape in all 4 states)
- [ ] T, S, Z, J, L pieces use a 3x3 bounding box
- [ ] Visual verification: each state matches the ASCII art in ENGINE.md
- [ ] `getBlocks(type, rotation)` returns the correct `Position[]` for any combination

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 1.5, Task 2.6, Task 3.4
