# Task 1.1: Project Scaffold

## Spec References
> No spec sections needed — this is project setup.

## Prerequisites
- [ ] None — this is the first task

## Description
Initialize the project with Vite, React, and TypeScript. Set up the directory structure, configuration files, and verify the dev server and production build both work.

## Files
| Action | Path |
|--------|------|
| Create | `package.json` |
| Create | `tsconfig.json` |
| Create | `vite.config.ts` |
| Create | `index.html` |
| Create | `src/main.tsx` |
| Create | `src/App.tsx` |

## Acceptance Criteria
- [ ] `npm install` succeeds without errors
- [ ] `npm run dev` starts the Vite dev server and renders a blank React app
- [ ] `npm run build` produces a production build without TypeScript errors
- [ ] TypeScript strict mode is enabled

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 1.2, Task 5.4
