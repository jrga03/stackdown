# Task 2.7: Engine Barrel Export

## Spec References
> No specific spec section — this is a module organization task.

## Prerequisites
- [ ] Task 2.6 complete — all engine modules implemented
- [ ] Files exist: all `src/engine/*.ts` files

## Description
Create the engine's public API barrel file. Re-exports `GameEngine`, `EventBus`, all types, enums, and constants needed by external modules (renderer, input, UI). External consumers should import from `'../engine'` without reaching into internal files.

## Files
| Action | Path |
|--------|------|
| Create | `src/engine/index.ts` |

## Acceptance Criteria
- [ ] Exports `GameEngine`, `EventBus`, all types, all enums, and all constants
- [ ] External modules can import from `'../engine'` without reaching into internal files

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 4.5
