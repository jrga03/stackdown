# Task 6.4: Audio System (Stub)

## Spec References
> No spec section — this is a stub for future implementation.

## Prerequisites
- [ ] None — standalone module

## Description
Create the audio system interface as no-op stubs. Defines the Sound enum and AudioManager class with play, setVolume, mute, unmute methods. No actual audio files or playback — just the interface ready for future implementation.

## Files
| Action | Path |
|--------|------|
| Create | `src/audio/AudioManager.ts` |
| Create | `src/audio/sounds.ts` |

## Acceptance Criteria
- [ ] `AudioManager` class with `play(sound)`, `setVolume(v)`, `mute()`, `unmute()` methods
- [ ] Methods are no-ops (stubs) — no actual audio
- [ ] Sound enum: MOVE, ROTATE, LOCK, LINE_CLEAR, QUAD, TSPIN, LEVEL_UP, GAME_OVER
- [ ] Interface ready for future implementation without changes to calling code

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: none
