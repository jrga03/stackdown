# Task 3.2: BlockRenderer

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/RENDERER.md` → "Block Rendering" (lines 16-63)

## Prerequisites
- [ ] Task 3.1 complete — color palette defined
- [ ] Files exist: `src/renderer/colors.ts`

## Description
Implement bevel-shaded block rendering. Each block is drawn procedurally with base fill, center gradient, 4 bevel trapezoids (top/left = highlights, bottom/right = shadows), specular highlight dot, and outer border. Bevel ratio is 0.15 of cell size.

## Files
| Action | Path |
|--------|------|
| Create | `src/renderer/BlockRenderer.ts` |

## Acceptance Criteria
- [ ] `drawBlock(ctx, x, y, cellSize, color)` draws a single block with bevel shading
- [ ] Block has: base fill, center gradient, 4 bevel trapezoids, specular highlight, outer border
- [ ] Bevel ratio is 0.15 of cell size
- [ ] Top/left bevels are light (highlights), bottom/right are dark (shadows)
- [ ] Specular dot positioned at (0.28, 0.28) relative to cell
- [ ] Outer border uses 0.5px offset for crisp lines
- [ ] Visual verification: rendered block has convincing 3D beveled appearance

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 3.3, Task 3.4
