# Task 4.3: KeyboardManager

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/INPUT.md` → "OS Key Repeat Rejection" (lines 186-221)
> - `docs/INPUT.md` → "KeyboardManager" (lines 241-265)
> - `docs/INPUT.md` → "Integration with Game Loop" (lines 268-280)

## Prerequisites
- [ ] Task 4.1 complete — `InputMapper` for key-to-action mapping
- [ ] Task 4.2 complete — `DASManager` for auto-repeat
- [ ] Files exist: `src/input/InputMapper.ts`, `src/input/DASManager.ts`

## Description
Handle keyboard events, reject OS repeat (`e.repeat`), and coordinate InputMapper + DASManager. Immediate actions (rotate, hard drop, hold, pause) fire directly. DAS-processed actions (left, right, soft drop) go through DASManager. Creates the barrel export for the input module.

## Files
| Action | Path |
|--------|------|
| Create | `src/input/KeyboardManager.ts` |
| Create | `src/input/index.ts` |

## Acceptance Criteria
- [ ] OS key repeats (`e.repeat === true`) are rejected
- [ ] `e.preventDefault()` called for mapped keys
- [ ] Immediate actions (rotate, hard drop, hold, pause) fire directly via callback
- [ ] DAS-processed actions (left, right, soft drop) go through DASManager
- [ ] `attach()` adds keydown/keyup listeners to window
- [ ] `detach()` removes listeners and clears DAS state
- [ ] No stuck keys after detach

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 4.5
