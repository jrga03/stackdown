# Task 5.10: Game Over Enhancements

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/UI.md` → "Menu Components — GameOverScreen"
> - `docs/UI.md` → "Personal Bests Persistence"
> - `docs/RENDERER.md` → "Text Popup System — Practice Mode: TIME'S UP! Popup"

## Prerequisites
- [ ] Task 5.5 complete — `GameOverScreen` component exists
- [ ] Task 5.9 complete — `usePersonalBests` hook for score persistence
- [ ] Files exist: `src/ui/screens/GameOverScreen.tsx`, `src/hooks/usePersonalBests.ts`

## Description
Enhance the `GameOverScreen` to support both game modes. Show "TIME'S UP!" heading for practice mode timeout and "GAME OVER" for topout. Display the game mode label and a "NEW PERSONAL BEST!" indicator when the score beats the stored best. Integrate `usePersonalBests` hook to check and update scores on game over.

## Files
| Action | Path |
|--------|------|
| Modify | `src/ui/screens/GameOverScreen.tsx` |
| Modify | `src/ui/screens/GameScreen.tsx` |

## Acceptance Criteria
- [ ] Heading shows "TIME'S UP!" when `reason === 'timeout'`
- [ ] Heading shows "GAME OVER" when `reason === 'topout'`
- [ ] Game mode label displayed (e.g., "Marathon" or "Practice")
- [ ] "NEW PERSONAL BEST!" indicator shown when `isNewBest` is true
- [ ] `GameOverScreen` accepts `gameMode`, `reason`, and `isNewBest` props
- [ ] `GameScreen` calls `checkAndUpdate(mode, score)` on game over and passes result as `isNewBest`
- [ ] Final stats still display score, level, and lines
- [ ] "PLAY AGAIN" and "MAIN MENU" buttons still function

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
