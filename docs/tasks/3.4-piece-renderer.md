# Task 3.4: PieceRenderer

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/RENDERER.md` → "Render Pipeline" (lines 7-14)
> - `docs/RENDERER.md` → "Ghost Piece Rendering" (lines 140-155)
> - `docs/RENDERER.md` → "Performance Notes — Hold and Next Queue" (lines 277-279)

## Prerequisites
- [ ] Task 3.2 complete — `BlockRenderer.drawBlock()` for drawing individual blocks
- [ ] Task 1.3 complete — `getBlocks()` for piece shape data
- [ ] Files exist: `src/renderer/BlockRenderer.ts`, `src/engine/Piece.ts`

## Description
Implement rendering for the active piece, ghost piece, and piece previews (hold and next queue). Active piece uses BlockRenderer for its blocks. Ghost piece renders at alpha=0.2 with a 1.5px outline. Piece previews are centered for display in side panels.

## Files
| Action | Path |
|--------|------|
| Create | `src/renderer/PieceRenderer.ts` |

## Acceptance Criteria
- [ ] Active piece renders all 4 blocks using `BlockRenderer.drawBlock` at correct grid positions
- [ ] Ghost piece renders with alpha=0.2 and 1.5px outline at piece color with alpha=0.5
- [ ] `drawPiecePreview(ctx, type, cellSize)` renders a centered piece for hold/next
- [ ] Piece positions correctly translated from grid to pixel coordinates
- [ ] Only visible-area blocks rendered (y >= 20 in the 40-row grid)

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 3.7, Task 5.2
