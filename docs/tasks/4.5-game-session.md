# Task 4.5: GameSession

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/UI.md` → "Canvas–React Boundary" (lines 62-81)
> - `docs/UI.md` → "useGameSession Hook — Throttling" (lines 139-152)
> - `docs/INPUT.md` → "Integration with Game Loop" (lines 268-280)

## Prerequisites
- [ ] Task 2.7 complete — engine barrel export (`src/engine/index.ts`)
- [ ] Task 3.7 complete — `GameRenderer` (`src/renderer/index.ts`)
- [ ] Task 4.3 complete — `KeyboardManager` (`src/input/index.ts`)
- [ ] Task 4.4 complete — `GameLoop` (`src/game/GameLoop.ts`)
- [ ] Files exist: `src/engine/index.ts`, `src/renderer/index.ts`, `src/input/index.ts`, `src/game/GameLoop.ts`

## Description
Connect engine, renderer, input, and game loop into a playable game session. Each tick: process DAS actions → engine.tick() → renderer.draw(). Throttles state updates to React at ~10fps (100ms interval). This is the integration point where the game becomes fully playable.

## Files
| Action | Path |
|--------|------|
| Create | `src/game/GameSession.ts` |

## Acceptance Criteria
- [ ] Constructor takes `HTMLCanvasElement` and creates engine, renderer, input, game loop
- [ ] Each tick: DAS actions → `engine.tick()` → `renderer.draw()`
- [ ] Input callback routes actions to `engine.applyAction()`
- [ ] `onStateUpdate(callback)` registers a throttled state callback (~10fps / 100ms)
- [ ] `start()` begins game loop and attaches keyboard listeners
- [ ] `destroy()` stops loop, detaches input, cleans up renderer
- [ ] `pause()`, `resume()`, `restart()` methods work correctly
- [ ] **Game is fully playable** — pieces fall, move, rotate, lock, lines clear, score tracks

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 5.1
