# Task 2.5: ComboTracker

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/ENGINE.md` → "Scoring — Combo Bonus" (lines 573-583)

## Prerequisites
- [ ] Task 1.2 complete — types defined
- [ ] Files exist: `src/engine/types.ts`

## Description
Implement combo counter logic. Starts at -1 (no active combo). Increments by 1 on each consecutive line clear. Resets to -1 when a piece locks without clearing. This is a simple state tracker used by ScoreManager and GameEngine.

## Files
| Action | Path |
|--------|------|
| Create | `src/engine/ComboTracker.ts` |

## Acceptance Criteria
- [ ] Starts at -1 (no active combo)
- [ ] Increments by 1 on each consecutive line clear
- [ ] Resets to -1 when a piece locks without clearing
- [ ] `getCombo()` returns current combo count
- [ ] After 3 consecutive clears, combo is 2 (started at -1, then 0, 1, 2)

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 2.6
