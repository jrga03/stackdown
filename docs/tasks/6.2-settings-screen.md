# Task 6.2: SettingsScreen

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/UI.md` → "Menu Components — SettingsScreen" (lines 296-304)
> - `docs/UI.md` → "State Management — Settings Persistence" (lines 328-347)
> - `docs/INPUT.md` → "Key Bindings" (lines 6-38)
> - `docs/INPUT.md` → "DAS/ARR Algorithm — Configuration" (lines 68-76)

## Prerequisites
- [ ] Task 5.4 complete — `Button` component and menu navigation pattern
- [ ] Files exist: `src/ui/common/Button.tsx`

## Description
Create the settings UI for key bindings and DAS/ARR configuration. Key bindings support "press a key" capture for rebinding. DAS/ARR use sliders/inputs with range constraints. All settings persisted to localStorage and survive page reload.

## Files
| Action | Path |
|--------|------|
| Create | `src/ui/screens/SettingsScreen.tsx` |
| Create | `src/ui/settings/KeyBindings.tsx` |
| Create | `src/ui/settings/GameSettings.tsx` |
| Create | `src/hooks/useSettings.ts` |

## Acceptance Criteria
- [ ] Key bindings display current mapping and allow rebinding by pressing a new key
- [ ] DAS delay configurable (slider or input, 50-300ms range)
- [ ] ARR interval configurable (slider or input, 0-100ms range)
- [ ] Settings persisted to `localStorage`
- [ ] Settings survive page reload
- [ ] "Reset to Defaults" button restores all defaults
- [ ] Settings applied to game without requiring restart

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: none
