# Task 5.1: GameScreen and useGameSession

## Spec References
> Read before starting. Only load the sections listed.
> - `docs/UI.md` → "useGameSession Hook" (lines 84-134)
> - `docs/UI.md` → "Component Tree" (lines 34-58)
> - `docs/UI.md` → "Canvas–React Boundary" (lines 62-81)

## Prerequisites
- [ ] Task 4.5 complete — `GameSession` class
- [ ] Files exist: `src/game/GameSession.ts`

## Description
Create the main game screen component and the hook that bridges GameSession to React. `GameScreen` renders a `<canvas>` element. `useGameSession` creates and manages the GameSession lifecycle, receiving throttled state updates (~10fps) for the HUD.

## Files
| Action | Path |
|--------|------|
| Create | `src/ui/screens/GameScreen.tsx` |
| Create | `src/hooks/useGameSession.ts` |

## Acceptance Criteria
- [ ] `GameScreen` renders a `<canvas>` element with a ref
- [ ] `useGameSession` creates a `GameSession` from the canvas ref
- [ ] State updates from engine received at ~10fps via throttled callback
- [ ] `gameState` includes: score, level, linesCleared, holdPiece, nextQueue, isPaused, isGameOver, combo, backToBack
- [ ] Session destroyed on component unmount (cleanup in useEffect)
- [ ] Canvas renders the game (pieces fall, player can interact)

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: Task 5.2, Task 5.3, Task 5.5
