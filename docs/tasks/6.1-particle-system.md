# Task 6.1: ParticleSystem

## Spec References
> No dedicated spec section — uses general renderer patterns from RENDERER.md.

## Prerequisites
- [ ] Task 3.5 complete — `AnimationManager` patterns for update/draw lifecycle
- [ ] Files exist: `src/renderer/AnimationManager.ts`

## Description
Add particle effects for hard drops and line clears. Particles have velocity, gravity, alpha decay, and finite lifetime. Hard drop impact spawns particles at the landing position. Line clear spawns particles across cleared rows. Dead particles are automatically removed.

## Files
| Action | Path |
|--------|------|
| Create | `src/renderer/ParticleSystem.ts` |

## Acceptance Criteria
- [ ] Hard drop impact spawns particles at the landing position
- [ ] Line clear spawns particles across cleared rows
- [ ] Particles have velocity, gravity, alpha decay, and finite lifetime
- [ ] `update(deltaMs)` advances particle physics
- [ ] `draw(ctx)` renders all active particles
- [ ] Dead particles are automatically removed

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: none
