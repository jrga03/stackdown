# Task 6.5: Utility Functions

## Spec References
> No spec section — general-purpose utilities.

## Prerequisites
- [ ] None — standalone module

## Description
Create shared utility functions used across modules. `clamp` constrains a number to a range, `lerp` linearly interpolates between two values, and `timer` provides simple elapsed time tracking. All functions are pure with no side effects.

## Files
| Action | Path |
|--------|------|
| Create | `src/utils/clamp.ts` |
| Create | `src/utils/lerp.ts` |
| Create | `src/utils/timer.ts` |

## Acceptance Criteria
- [ ] `clamp(value, min, max)` constrains a number to the given range
- [ ] `lerp(a, b, t)` linearly interpolates between a and b
- [ ] Timer utility provides simple elapsed time tracking
- [ ] All functions are pure with no side effects

## Post-Completion
- [ ] `npx tsc --noEmit` passes
- [ ] No circular imports
- [ ] Unblocks: none
